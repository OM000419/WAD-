<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Login Page</title>
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
      background: linear-gradient(to right, #f9f9f9, #e0e7ff);
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .login-box {
      background: #ffffff;
      padding: 35px 30px;
      border-radius: 15px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
      width: 100%;
      max-width: 360px;
      text-align: center;
    }

    .login-box h2 {
      margin-bottom: 25px;
      color: #333;
    }

    .login-box input {
      width: 100%;
      padding: 12px 15px;
      margin: 10px 0;
      border: 1px solid #ccc;
      border-radius: 8px;
      font-size: 15px;
      background: #f9f9f9;
      transition: border 0.3s, box-shadow 0.3s;
    }

    .login-box input:focus {
      border-color: #7aa9ff;
      outline: none;
      background: #fff;
      box-shadow: 0 0 5px rgba(122, 169, 255, 0.4);
    }

    .login-box input[type="submit"] {
      background-color: #7aa9ff;
      color: white;
      border: none;
      font-weight: bold;
      cursor: pointer;
      transition: background 0.3s;
    }

    .login-box input[type="submit"]:hover {
      background-color: #5f91ef;
    }

    .login-box .links {
      margin-top: 15px;
      font-size: 14px;
      color: #666;
    }

    .login-box .links a {
      color: #5f91ef;
      text-decoration: none;
      margin-left: 5px;
    }

    @media (max-width: 480px) {
      .login-box {
        padding: 25px 20px;
      }
    }
  </style>
</head>
<body>
  <div class="login-box">
    <h2>Login</h2>
    <form>
      <input type="email" placeholder="Email" required />
      <input type="password" placeholder="Password" required />
      <input type="submit" value="Login" />
    </form>
    <div class="links">
      Don't have an account?
      <a href="#">Register</a>
    </div>
  </div>
</body>
</html>
